# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Build output
dist/
dist-types/
build/
*.tsbuildinfo

# This repo is a standalone yarn workspace: yarn.lock IS committed so
# `git clone && yarn install` is reproducible. Reject stray npm/pnpm locks.
package-lock.json
pnpm-lock.yaml

# Test / coverage
coverage/
.nyc_output/

# Logs
*.log
*.pid
demo/.backstage-app/
demo/.ark-installed-by-demo
demo/.kagent-crds-installed-by-demo
demo/.kagent-installed-by-demo
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Local-only notes — never publish strategy/planning docs
*.local.md
AGENTS.local.md
docs/superpowers/
outputs/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~
